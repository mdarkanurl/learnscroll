import {
    users,
    refresh_tokens
} from "./auth.schema";
import { profiles, privacySettings } from './users.schema';
import { instructors } from "./instructors.schema";

export {
    users,
    refresh_tokens,
    profiles,
    privacySettings,
    instructors
}
